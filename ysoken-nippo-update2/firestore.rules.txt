rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // アプリからの匿名サインイン済みアクセスのみ読み書きを許可
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
